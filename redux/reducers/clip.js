'use strict'

const clip = (state = {hello: 'cosmos'}, action) => {
  switch (action.type) {
    case 'ACTION_TYPE':
      return
      break;
    default:
      return state
  }
}

export default clip
